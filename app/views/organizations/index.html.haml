%table
  %thead
    %th Organization ID
    %th Name
    %th Status
  %tbody
    -@orgs.each do |org|
      %tr
        %td= org.collegiatelink_id
        %td= link_to org.name, org
        %td= org.status
